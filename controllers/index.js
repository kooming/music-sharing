const userController = require('./user.controller')
const musicController =require('./music.controller')
const likeController = require('./like.controller');
const liveController = require('./live.controller');
const playlistController = require('./playlist.controller')
const postController = require('./post.controller');

module.exports = {userController, musicController, liveController,likeController,postController, playlistController}
